<table>
    <tr>
        <th>Coupon Code</th>
        <th>Store</th>
    </tr>

    <% @coupons.each do |coupon|%>
    <tr>
        <td><%= coupon.coupon_code %> </td>
        <td><%= coupon.store%> </td>
        <td><%= link_to 'Show', coupon_path(coupon)%></td>
        <td><%= link_to 'Edit', coupon_path(coupon)%></td>
    </tr>
    <%end%>


</table>

<%= link_to 'Create a Coupon',new_coupon_path %>
